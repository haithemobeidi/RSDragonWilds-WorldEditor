# Master Handoff Index

A catalog of all session handoffs. Newest entries at the top. Each entry is a one-line summary — read the full handoff document for details.

---

**Handoff [04-08-2026_02-45-02_EDT](handoffs/04-08-2026_02-45-02_EDT.md)** — 6th handoff of the 04-07/04-08 marathon. 🎯 **Built dedicated world editor UI.** Standalone Flask app `world_editor.py` on port 5001 with single-page UI (`templates/world_editor.html`). Sidebar world list + detail panel with action cards for transplant, clone, mode toggle. Friendly piece names via `KNOWN_PIECE_NAMES` dict. Packaged surgical transplant into `parser.py` as `WorldSave.transplant_structures_from()` with proper helper `_find_gbm_nobj_layout()` (recursive chunk walker handling LEVL 8-byte version header quirk and NOBJ ClassID-at-start quirk). Auto-clears SpudCache, auto-backups via `_raw_save()`. Clone endpoint generates new WorldSaveGuid + rewrites WorldName/WorldSlotName FStrings with chunk-length fixup for length deltas. Fixed UX issue: initial labeling "Transplant From Another World" was ambiguous about direction; reworked to explicit FROM (donor) → INTO (this world) labels. Fixed WSL2 networking gotcha (server bound to 127.0.0.1 only, Windows browser couldn't reach; fixed by binding 0.0.0.0). User got the editor running and tested. **MAJOR USER TESTING FINDINGS:** world mode doesn't matter for transplants (Custom→Standard works). Refined Category 1/2 model into 1A (pure meshes — walls/floors/roofs work fully), 1B (visible but broken collision — doors and water barrels you walk through), 1C (partially functional — farming plots can be dug), 2 (invisible — chests, anvils, rune altars, all "machines" with state). Need to verify wall collision next session to confirm theory. Closes tasks #8, #15, #18, #20. Status: world editor working, surgical transplant feature shipped, user actively using it.

**Handoff [04-08-2026_01-30-59_EDT](handoffs/04-08-2026_01-30-59_EDT.md)** — 5th handoff of the 04-07/04-08 marathon. 🎯 **CROSS-WORLD SURGICAL TRANSPLANT WORKING END-TO-END.** Built parameterized transplant scripts (`surgical_transplant_v2.py`, `v3.py`). First attempt failed — pieces silently dropped by game. Debugged via "piece dupe test" which succeeded, proving our Pces injection mechanics were sound. Found the root cause: `GlobalBuildingManager` NOBJ's **PROP[2] uint32 counter** must be updated to at least `max(target, source)` when injecting pieces, or the game rejects them on load. With the counter fix, cabin transplanted from DiffTest → TransplantTest and rendered in-game. User confirmed twice ("cabin is there, definitely works"). Also discovered **Category 1 vs Category 2 structure split** with hard byte evidence: chest's position doubles appear at TWO different offsets in DiffTest (Pces at 0xb342 AND a 589-byte SPWN record at 0xef3f inside L_World.SATS). Chests = Pces + SPWN, walls = Pces only. Our Pces-only transplant works for Category 1 but silently drops Category 2 chests. Wrote **comprehensive `docs/DRAGONWILDS_SAVE_FORMAT_GUIDE.md`** for the modding community (user is in a Dragonwilds modding Discord and plans to share). Covers SPUD chunks, empirical quirks, working transplant recipe, hex examples, Category 1/2 distinction with evidence. Empirical quirks documented: LEVL body has 8-byte version header after name; NOBJ ClassID is at start not end of body; `0a 02 00 00 f9 03 00 00` is a universal SPUD marker. Next session: package v3 into parser.py as proper method, add collision check, research Category 2 (SPWN) transplant, clone world feature. Memory saved: don't editorialize about session length.

**Handoff [04-07-2026_23-34-44_EDT](handoffs/04-07-2026_23-34-44_EDT.md)** — 4th handoff of the day. 🎯 **Full structural decoding of cabin storage.** Read SPUD source (`SpudData.h`) for the struct layouts of `FSpudLevelData`, `FSpudObjectData`, `FSpudNamedObjectData`. Built `diff14.py` — a recursive LEVL/LATS/NOBJ parser. Empirical quirks found: LEVL body has 8-byte version header (`0a 02 00 00 f9 03 00 00`) between the Name FString and sub-chunks; NOBJ body puts `ClassID` at the START not end. After fixing both, walked all NOBJs in all LEVLs of D and E — D: 332 NOBJs, E: 457 (+125). Surprise: NONE of the +125 new NOBJs are cabin pieces; they're all ore nodes/NPCs in the streaming cells. Cabin pieces aren't individual NOBJs at all. **Traced Pces chunk offset (0xb308 in E) through the chunk hierarchy** and discovered it's INSIDE `CUST` which is INSIDE `GlobalBuildingManager`'s NOBJ body in L_World's LATS. The entire cabin state (all 33 pieces) is contained in ONE NOBJ record — `GlobalBuildingManager_UAID_CC96E50CA869C0EC01_1097795184`. Its body: ClassID(13) + FString Name + 12B prop metadata + 8B version + CORA(159B) + PROP(53B, 4 counters) + CUST(7857B) → containing the Pces chunk. Math validates: cabin delta = +7,314 B in Pces = +7,314 B in GBM NOBJ = +7,307 B in LATS. Surgical transplant recipe is now clear: **modify one NOBJ + update 8 chunk-length headers up the tree** (Pces → CUST TArray count → CUST length → NOBJ length → LATS → LEVL → LVLS → SAVE). Next (continuing immediately): build `extract_cabin_data()` + `inject_pces_records()`, test against restored Middle Eearth. User feedback saved to memory: don't editorialize about session length. User not done. Status: structural understanding complete, implementation next.

**Handoff [04-07-2026_22-42-32_EDT](handoffs/04-07-2026_22-42-32_EDT.md)** — 3rd handoff of the day (checkpoint, user not done). 🎯 **NUCLEAR TRANSPLANT SUCCESS.** First surgical attempt (injecting one cabin wall into Middle Eearth's Pces chunk) failed — game loaded but didn't instantiate the wall. Game log revealed: `LogBuildingSaveData: Error: Failed to get 'BuildPieceData' for Persistence ID '' loading Building Piece from SPUD`. **Meta-breakthrough: identified SPUD as the save system** (Steve's Persistent Unreal Data, open-source UE plugin at github.com/sinbad/SPUD). Fetched docs + full chunk tag list from `SpudData.h`. Inventoried the full D→E delta across ALL SPUD chunks (not just our hardcoded subset) and discovered the cabin is stored across **+125 NOBJ, +128 PROP, +128 CORA, +45 CDEF, +4 LEVL** chunks — not just Pces. Pces is Dragonwilds-custom (not a SPUD primitive) and stores only placement metadata; the per-piece persistent state lives in NOBJ/PROP/CORA records. Located the cabin's home: the single `L_World` master LEVL chunk (not the per-cell streaming chunks). **Nuclear experiment: cloned E_with_cabin wholesale** — copied over a new filename, rewrote internal WorldName (3 occurrences), generated a fresh WorldSaveGuid (replaced 2 occurrences of the old GUID), wiped SpudCache. User relaunched game — **CabinMid world shows up AND the cabin materialized end-to-end** ✓. Shipped `get_placed_pieces()` in parser.py (+111 lines) — the cross-world-portable Pces scanner. Verified on D/E test corpus and Gielinor (318 pieces, 35 classes). Structure data IS portable between worlds given matching filename + WorldName + WorldSlotName + unique WorldSaveGuid + clean SpudCache. User confirmed: cabin visible in game. Next: package the nuclear recipe as `WorldSave.clone_to_new_name()`, then work toward surgical sub-chunk transplant.

**Handoff [04-07-2026_17-47-37_EDT](handoffs/04-07-2026_17-47-37_EDT.md)** — Continuation of the 17:07 handoff (same calendar session, ~40 min later, breakthrough warranted a separate log). 🎯 **CROSS-WORLD DETECTION SOLVED.** First test failed (world-local class-table indices in `body_first4` and `class_ref` differ between DiffTest and Gielinor). Side-by-side hex comparison of two empty Ash Chests in two different worlds revealed the **portable signature**: `body_length` + `component_count` (uint32 at offset 0x4d) + the constant 9-byte property header `01 0a 02 00 00 f9 03 00 00` at offset 0x44. The 5×4 = 20 bytes of class-table indices that follow are world-local and intentionally ignored. Rewrote `KNOWN_STRUCTURES` and `get_placed_structures()` accordingly. Verified end-to-end: detector now finds the fresh Ash Chest in **Gielinor cross-world** (a different save with different CDEF/CNIX layout) while still detecting all 3 chests in DiffTest D, with zero false positives. **Major side discovery:** the 109 "containers" we find via `get_containers()` JSON-section parsing are crafting stations / smelters / cooking pots — NOT player chests. The user's actual chests with 20+ items live somewhere other than `SPWN`, possibly in `Pces` (building pieces) chunks or a chunk-streamed subsave we haven't explored yet. User refined the goal: focus on structure transplant only (not contents — those can be transferred via character inventory). Next session target: **Middle Eearth** (not Gielinor — user's choice). User is building a small hut in DiffTest as a multi-structure transplant test case. Status: detection working, transplant pipeline next. User confirmed: yes (Gielinor ash chest position output matches their placement to within 233 cm).

**Handoff [04-07-2026_17-07-26_EDT](handoffs/04-07-2026_17-07-26_EDT.md)** — Phase 1 (quest fingerprinting) paused after discovering FTUE templates have no PersistenceID/variables (need a different .pak export pass — Blueprint graphs, not just QuestData properties). Pivoted to **Phase 3 (structure transplant)** — community request to move chests between worlds. Massive reverse-engineering session: mapped the entire chunked .sav format (`SAVE`/`INFO`/`CINF`/`GLOB`/`CNIX`/`PNIX`/`CDEF`/`LVLS`/`SPWN`/`PROP`/`Pces`/`SATS`/`CORA` chunks), captured the placed-actor record layout (Personal Chest = 589 B with instance GUID at 0x04, 6 transform doubles at 0x14, class-ref at 0x44; Ash Chest = 625 B with 5 components vs Personal's 3). **Critical finding:** same chest in two saves of the same world differs by only 2 tick/hash bytes — cross-world transplant is much simpler than expected. Shipped `WorldSave.get_placed_structures()` in `parser.py` (+118 lines) — verified byte-perfect on the A/B/C test corpus and non-crashing on 3.6 MB live Gielinor (363 SPWN records, 0 personal chests since user hasn't unlocked recipe). Built `scripts/structure_research/` with 4 test snapshots + 8 progressively smarter diff scripts. **Free side discovery:** root cause of Bug #001 — every chest carries `OwnerCharacterGuid` baked in, so converting world owner orphans them; fix path now obvious. Status: detection layer working, cross-world index stability unverified (user testing in parallel by placing an Ash Chest in Gielinor). Next session: add Ash Chest signature, validate cross-world detection, build transplant function. User confirmed: yes (test corpus and Gielinor counts match expectations).

**Handoff [04-07-2026_00-34-43_EDT](handoffs/04-07-2026_00-34-43_EDT.md)** — 5th session of the marathon. **PHASE 2 COMPLETE**: extracted the game's .pak files via FModel + community .usmap, built a GUID → name map with 947 entries (items / quests / attributes / skill perks / consumables / equipment / food / plans / potions), wired into the parser so the editor now shows real friendly names ("Goblin Diplomacy", "Restless Ghosts", etc.) for quests and items instead of base64 GUIDs. Also: UI Pass 4 source-level cleanup (replaced all ~43 inline font-size strings with semantic typography.css classes, removed the !important mega-block), manual GUID override system for runtime-instance GUIDs that don't match templates, quest dedupe in parser. Current coverage: 7/15 quests on test save (the 8 unmapped are runtime-instance GUIDs — need variable-fingerprinting matcher next session). Bug #002 effectively resolved. Next: improve quest coverage, extract item icons from .pak, map spells/mounts, fix bug #001 (ownership binding).

**Handoff [04-06-2026_20-38-36_EDT](handoffs/04-06-2026_20-38-36_EDT.md)** — Fourth session of the day. Massive polish + feature pass. **Full frontend refactor** (1743-line monolith → 16 focused files, largest 317 lines). **Database tab** with reference catalog imported from Ashenfall's Completionist Log XLSX (22 quests, 197 items) + source attribution. **Wiki icon scraper** — fetched 159/197 item icons (81%) via MediaWiki API with manual override support for XLSX typos. **Game-style UI theme** applied then flattened per user feedback, with aggressive font-size `!important` overrides to force readability past inline-styled tiny text. **BUGS.md tracker** created with community-reported bug #001 (conversion breaks locked chests / protection totems — PROP offset mismatch suspected, workaround warning shipped). **Portability fix** — removed hardcoded user paths so anyone can clone+run. Repo publicly shipped at https://github.com/haithemobeidi/RSDragonWilds-WorldEditor. 7 commits pushed. Next session: Phase 2 — extract game .pak files for GUID → name mapping (unlock friendly names in Inventory/Quests/Spells/Mounts tabs).

**Handoff [04-06-2026_18-27-45_EDT](handoffs/04-06-2026_18-27-45_EDT.md)** — 🎯 BREAKTHROUGH. Found the persistent custom-mode flag: first byte of `CustomDifficultySettings` PROP field. **Both bytes (L_World+9 enum AND PROP byte)** must be set together; setting just one fails. Successfully converted Gielinor to Custom permanently — verified by user playing main character (Serious_Beans, char_type=3) on it with custom difficulty applied. Built the conversion features into the GUI: World Mode display + Convert/Revert buttons per world, plus Character "World Compat" dropdown. Updated parser.py with `convert_to_custom`/`revert_to_standard` methods and `set_char_type`. Code committed and pushed to new GitHub remote `RSDragonWilds-WorldEditor`. Status: ✅ User goal achieved end-to-end.

**Handoff [04-06-2026_17-37-14_EDT](handoffs/04-06-2026_17-37-14_EDT.md)** — Continuation. Verified `char_type=3` mutually exclusive with standard worlds. **DISCOVERED the world difficulty enum byte at L_World+9** (0=Standard, 1=Hard, 3=Custom). Successfully converted Gielinor from Standard to Custom with a single byte flip; user played main character on custom Gielinor for a full session. **HOWEVER game's auto-save reverts the enum byte to 0 each time** while preserving structural changes. Open question for next session: is there ANOTHER persistent custom-mode flag we haven't found, OR do we need to re-flip the enum before every play session? Created QUICK_REFERENCE.md cheat sheet. End state: enum reverted by game, char_type=3 still set.

**Handoff [04-06-2026_16-48-31_EDT](handoffs/04-06-2026_16-48-31_EDT.md)** — TWO MAJOR DISCOVERIES. (1) Difficulty "secondary copy" found at `L_World+10` — game reads from a tagless float array there, NOT from named entries. Direct hex edit confirmed working both at UI and gameplay level on Middle Eearth (crafting cost reduced to 0.1× as set). (2) `meta_data.char_type` field gates custom-world access — changing main character from `0` → `3` allowed Serious_Beans to join Middle Eearth (custom world) with all data intact. Also created README.md + run.bat one-click launcher. Status: working, **but `char_type=3` impact on standard worlds (Gielinor) is UNTESTED — critical first task next session**. parser.py difficulty methods still target wrong location and need rewrite. Next session goal: transfer Gielinor's storage chest contents to a custom world.

**Handoff [04-06-2026_03-41-14_EDT](handoffs/04-06-2026_03-41-14_EDT.md)** — Initial session: built Flask save editor from scratch, reverse-engineered character JSON + binary world `.sav` formats, implemented full character editing (skills/inventory/quests/spells/mounts/position/status effects) and partial world editing (chests/weather/events), reverse-engineered all 35 custom difficulty tags and built Phase 1 difficulty editor. Status: **working at file level**, but Phase 1 difficulty edits **do not apply in-game** (diagnostic test plan in TODO_TOMORROW.md). User confirmed: character editing untested but parser pulls correct data; difficulty edit failed in-game test.
